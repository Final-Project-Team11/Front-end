export interface CardProps {
  tab?: boolean;
}

export interface CardInfoType {
  userId: string;
  userName: string;
  profileImg: string;
  birthDay: string;
  phoneNum: string;
  joinDay: string;
}

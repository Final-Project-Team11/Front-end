import styled, {css} from "styled-components";
import * as interfaces from "./interfaces";

const sieze : interfaces.Sieze = {
    small : {
        width : '100px',
        height : '25px'
    },
    medium : {
        width : '200px',
        height : '50px'
    }
}